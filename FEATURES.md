## Member Records

* Member records — custom database record type for members with profile fields
* Application workflow — multi-step application process with status transitions
* FE-User link — connect member records to TYPO3 frontend users (requires `mai_account`)

## Backend & Frontend

* Backend module — manage members and applications in the TYPO3 backend
* Frontend listing — display member lists and profiles on the frontend
